# Day 2 Notes

## 今日实际学习时长

- 开始时间：17.00
- 结束时间：23.00
- 有效学习时长：4h

## CSV

1. `csv.DictReader` 返回的数据结构是什么？答：返回迭代器
2. 为什么读取 CSV 时要使用 `newline=""`？答：让 csv 模块自己处理换行，避免文本模式的换行转换干扰 CSV。
3. 为什么要显式指定 `encoding="utf-8"`？答：指定的是字符编码
4. 如何检查 CSV 表头？答：fieldnames
5. 多余字段和缺失字段在 `DictReader` 中可能如何表现？答：None和restkey

## 文件与路径

1. `Path(__file__).resolve().parent` 表示什么？答：父目录
2. `mkdir(parents=True, exist_ok=True)` 的两个参数分别有什么作用？答：parents表示创建父目录，exitok表示如果已经存在也不报错。
3. 为什么不建议在代码中写死 `D:\Code\...` 这样的绝对路径？答：不方便移植代码

## 异常处理

1. `ValueError` 在本项目中表示什么？答：数值错误
2. `FileNotFoundError` 在什么情况下出现？答：表示要访问的文件或目录路径不存在
3. 为什么禁止使用裸 `except:`？答：捕获范围太广，容易隐藏程序错误
4. `raise` 和 `return` 的区别是什么？答：raise直接终止程序，return返回值

## 字典与哈希

1. Two Sum 的哈希表保存什么？答：保存已经遍历过的数据
2. Valid Anagram 的字典保存什么？答：读取过的字母和数量
3. 字典查找为什么平均可以看作 O(1)？答：哈希表

## 今日遇到的三个错误及解决办法

1.没有创建父目录就打开文件
2.Anagram中把字典当做列表使用
3.没有检查restkey

## 今日最难的函数

- 函数名称：load_students
- 难点：代码长，涉及知识点多，查文档频繁
- 最终如何解决：查文档
- AI 帮助了哪些部分：查文档
- 哪些代码是自己独立重新写出的：全部

## 明日改进

1.代码熟练度
2.敲python代码的速度
3.查文档的速度
